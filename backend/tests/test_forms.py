from django.test import SimpleTestCase
# from backend.forms import *

# class TestForms(SimpleTestCase):

    # def test_[...]_form_valid_data(self):
    #     form = [SpecificForm](data={
    #         [json]
    #     })
    #     self.assertTrue(from.is_valid())

    # def test_[...]_form_no_data(self):
    #     form = [SpecificForm](data={})
    #     self.assertFalse(from.is_valid())
    #     self.assertEquals(len(form.errors), [number of key/values that should've been in the form])
